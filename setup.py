from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    author='INSERT NAME, Federal Reserve Bank of Cleveland',
    author_email='INSERT EMAIL',
    license=open('LICENSE.txt').read(),
)
